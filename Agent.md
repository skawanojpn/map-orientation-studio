# Map Orientation Studio — 与件ドラフト

## 1. プロジェクト概要

`axis-mapping` と `azi-rotate` を統合し、地図の中心・向き・縮尺・投影法を操作しながら、地理的な距離・方向・歪みを観察できるブラウザアプリを作る。

仮称は **Map Orientation Studio**。既存アプリの機能を単純に足し合わせるのではなく、「同じ中心・同じ回転条件で投影法を比較する」ことを中心体験とする。

## 2. 既存アプリの把握

### axis-mapping

- D3.js v7、TopoJSON、Canvas描画。
- `d3.geoMercator()` を使用し、経度・緯度・ロール・縮尺を操作。
- 中央軸、斜軸グリッド、標準経緯度線、都市名をレイヤーとして表示。
- ドラッグで中心移動、Shift+ドラッグでロール、ホイールで拡大縮小。
- 都市プリセット、距離表示、5テーマ、日英切替、PNG出力。
- `src/index.html` と `docs/index.html` が同一の単一HTML構成で、GitHub Pages向け。

### azi-rotate

- D3.js v7、TopoJSON、Canvas描画。
- `d3.geoAzimuthalEquidistant()` を使用し、選択地点を中心にした正距方位図を表示。
- 国境線、都市名、等距離円、経緯度網をレイヤーとして表示。
- ドラッグ、Shift+ドラッグ、ホイール、タッチのピンチ操作に対応。
- 都市プリセット、距離表示、5テーマ、日英切替、PNG出力。
- `land` と `countries` の両方を使い、国土塗りつぶしと国境線を分離。
- 読み込み失敗時のステータス表示、Escapeで設定を閉じるなど、操作面はaxis-mappingより整理されている。

### 統合時に引き継ぐ知見

- 既存の都市プリセット、テーマ、日英ラベル、Canvasの高DPI対応、PNG出力を再利用する。
- 正距方位図の等距離円と、斜軸グリッド／中央軸を同じレイヤー管理に統合する。
- 投影法を切り替えても「中心地点」「ロール」「縮尺」「表示言語」「テーマ」の状態を一貫させる。
- CDN上の地図データに依存するため、読み込み失敗表示と将来のローカルデータ同梱余地を残す。

## 3. 必須要件（MVP）

1. 世界地図を表示し、地図データの読み込み状態・エラー状態を表示する。
2. 次の投影法を切り替えられる。
   - メルカトル（比較用の標準的な世界地図）
   - 正距方位図法（中心からの距離・方位の観察）
   - 追加投影法は拡張可能な登録方式にする。
3. 中心経度・中心緯度・ロール・縮尺を数値入力できる。
4. ドラッグ、Shift+ドラッグ、ホイール、タッチピンチで操作できる。
5. 都市プリセットを地域別に選択できる。東京など既存都市を引き継ぐ。
6. 都市名、国境線、標準グリッド、斜軸グリッド、中央軸、等距離円を個別に表示／非表示にできる。
7. マウスポインタ位置または選択地点に関する距離情報を表示する。
8. Classic、Cyber、Military、Nature、Paperのテーマを引き継ぐ。
9. 日本語／英語を切り替えられる。
10. 現在の地図をPNGとして保存できる。
11. VercelまたはGitHub Pagesで公開可能な静的ビルドを作る。
12. GAS Webアプリとしても動かせる配布形態を設計する。

## 4. 改善方針

- 巨大な単一HTMLから、状態・投影・描画・UI・データを分離する。
- 地理座標（経度・緯度）とD3の回転配列の符号変換を一箇所に集約する。
- レイヤー描画順を固定し、投影法ごとの差分はProjection Adapterに閉じ込める。
- 数値入力の範囲、NaN、極点付近、日付変更線付近を安全に扱う。
- Canvasの実ピクセルサイズとCSSサイズを分離し、高DPIでもPNG品質を維持する。
- キーボード操作、フォーカス表示、ラベル、コントラスト、モーダルのEscape操作を整える。
- 地図データ・外部CDNが使えない場合に、利用者が原因を理解できる状態にする。

## 5. 公開・配布方針

### 静的公開

- 第一候補はVercel。ビルド、プレビュー、環境変数、将来のAPI追加が扱いやすい。
- GitHub Pagesは、リポジトリ内の静的出力をそのまま公開する代替経路とする。
- フロントエンドは外部サービスに依存しないコア状態管理を持つ。

### GAS

- GAS版はHtmlServiceで画面を配信し、必要に応じてGoogleアカウント単位の設定保存・共有・ログ記録を追加できる構成にする。
- 静的版とGAS版で地図描画ロジックを共通化し、GAS固有処理をAdapterに限定する。
- GASのHTMLテンプレート、`doGet`、デプロイ手順、権限・実行ユーザーの注意点をドキュメント化する。
- 座標や地図画像などの個人情報を保存する機能を追加する場合は、保存範囲と削除方法を明示する。

## 6. 追加機能候補

優先度の高い候補は、投影法の左右比較、URLへの状態保存・共有、複数地点の距離比較、表示設定のプリセット保存、GeoJSON／CSVの地点インポート、SVG／高解像度PNG出力、地図上の地点クリックによる詳細表示である。

学習用途を強化する候補として、投影法ごとの性質（正距・正角・正積など）の説明、歪みのヒートマップ、Tissotの指示楕円、方位線・大圏航路・等距離円の比較、時差・日照境界線の表示を検討する。

## 7. 完了条件

- 既存2アプリの主要機能を失わず、投影法を切り替えて比較できる。
- デスクトップとモバイルで操作できる。
- 静的公開版とGAS版の両方で地図表示・操作・PNG出力が確認できる。
- 初回利用者が操作方法と投影法の違いを理解できる。
- `Plan.md`の検証項目を満たし、既知の制約をREADMEに残す。
