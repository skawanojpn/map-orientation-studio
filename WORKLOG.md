# 作業状況

> 現在の方針: 海位・地形水深機能は撤去済み。既存の地図機能を安定版として維持する。

## 2026-08-16: 都市検索・都市データを拡張

- 世界各地域の首都・主要都市を追加。
- 歴史都市・文化都市を検索用データとして追加。
- プルダウンは主要都市中心、検索入力時は全登録都市を対象に変更。
- 日本語名・地域グループを追加。
- 中心地点の分類フィルタと、地図上の都市名分類フィルタを分離。
- 首都・主要都市・歴史都市・港湾都市・観光都市を分類条件として追加。
- 海位機能は再導入していない。

## 2026-08-15: 地形・水深／海面変化機能の開始

### 開始前のGitHubチェックポイント

- リポジトリ: https://github.com/skawanojpn/map-orientation-studio
- 公開先: https://skawanojpn.github.io/map-orientation-studio/
- ベースコミット: `5de8c7c` (`Add equal-area projection guides and map projections`)
- ベース状態: `git status --short` が空であることを確認
- ベース機能: 8種類の投影法、都市表示、2地点比較、投影法解説、GitHub Pages公開

### 今回の目的

既存の投影法を増やすのではなく、標高・水深データと海面高度を現在の地図へ重ねるレイヤー機能を追加する。

### 実装方針

1. 既存地図と投影法を維持する。
2. 地形・水深を低解像度の色分けレイヤーとして描画する。
3. 海面高度を `-125m` から正の値まで変更できるようにする。
4. 設定した海面より低い地点を海域として扱い、変更後の海岸線・水没域を表示する。
5. 高解像度全球データをリポジトリへ直接同梱せず、まずは軽量なサンプル／低解像度データで成立性を確認する。

### 失敗時の復旧

今回の作業で問題が発生した場合は、ベースコミット `5de8c7c` の状態を基準に修正する。既存機能を壊す変更は分離してコミットし、ビルド成功後にのみ公開成果物を更新する。

### リスク・未確定事項

- 全球の高解像度標高・水深データは容量が大きく、GitHub PagesやGASへの直接同梱には不向き。
- まずは表示方式と操作性を検証し、データソース・解像度・タイル配信方式は別途決定する。
- 海面を過去へ戻す単純モデルは教育・可視化用であり、氷床荷重や地殻変動を含む厳密な古地理復元ではない。

## 進捗

- [x] 現状確認
- [x] GitHub上のベースコミット確認
- [x] 作業方針と復旧方針を記録
- [x] 地形・水深レイヤー（撤去済み）
- [x] 海面高度スライダー（撤去済み）
- [x] 現在／氷期／将来プリセット（撤去済み）
- [x] ビルド・型チェック
- [x] GitHub Pages反映確認（公開ビルドのコミット `1296c3f` が `built`）

## 2026-08-15: 海面高度の変化が見えない問題を修正

- 海面高度スライダー／プリセット操作時に地形レイヤーを自動的に有効化。
- 海面上昇で水没する低地、氷期に現れる浅海域、深海を別色で表示。
- 現在の海岸線を白線で重ね、海面変更前後を比較可能にした。
- `npm run build` 成功。

## 2026-08-15: 地形レイヤーが全て青になる問題を修正

- TopoJSON変換後のFeatureラッパーを明示的にGeometryとして`geoContains`へ渡すよう修正。
- 陸地セルと海域セルの判定を分離して保持。
- `npm run build` 成功。コミット: `db058ab`

## 2026-08-16: 海位差分表示を再設計中（未公開）

- 通常の陸地ポリゴンを保持し、海面高度の変更差分だけを別描画する方式へ変更。
- 海岸セルに低い仮想標高を設定し、`+1m`〜`+10m`で水没差分、`-125m`で露出差分が視認できるようにした。
- 海面高度の設定値と、水没セル数・新たな陸地セル数を同じ読出し欄へ表示。
- `npm run build` 成功。
- この変更は未公開。GitHub Pagesへは、ローカルで海位変化を確認してから反映する。

## 2026-08-16: NOAA ETOPO実データ接続（未公開）

- NOAA NCEI ETOPO 2022の60秒全球グリッドから、5度間隔のサンプルを取得する処理を追加。
- 取得成功時はセルの標高・水深をETOPO値へ置換し、取得失敗時は概念モデルへフォールバック。
- UIに「実データ／読み込み中／概念モデル」の状態表示を追加。
- 実データの取得確認と海位差分の表示確認が完了するまで公開しない。

## 2026-08-16: 海位機能をオミット

- 海位・地形水深機能は、実データに基づく信頼できる表示品質へ到達していないため撤去。
- 海面高度UI、地形・水深レイヤー、海位差分メッシュ、ETOPO取得処理を削除。
- 既存の投影法、都市、2地点比較、投影法解説は維持。
- 今後再開する場合は、地域限定の事前処理済み実データを検証してから再設計する。

### 実装メモ

- `地形・水深（概念モデル）`レイヤーを有効にすると、標高・水深を色分けした低解像度メッシュを既存の投影法で描画する。
- 海面高度が変わると、メッシュの海域・陸域判定が変わる。現在の海岸線と比較するための基礎機能であり、実測DEMそのものではない。
- 次の段階では、GEBCO等の標高・水深データを5度メッシュへ変換するインポート処理へ差し替える。
- `npm run build` 成功済み。`dist/`、`docs/`、`gas/Index.html`を生成済み。
