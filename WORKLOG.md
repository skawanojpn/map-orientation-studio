# 作業状況

> 現在の方針: 海位・地形水深機能は撤去済み。既存の地図機能を安定版として維持する。

## 2026-08-16: 中心地点操作のユーザビリティ改善

- 操作順を「地点を検索 → 地点の種類で絞り込み → 地点を選択」と明示。
- 「中心地点の分類」を「地点の種類」に変更し、軍事・歴史地点など都市以外も自然に扱える表現へ変更。
- 地図側の分類名も「地図ラベルの種類」に統一。
- 検索欄・地点選択欄に明示ラベルを追加。
- サイドバー幅、文字サイズ、入力欄・ボタンの高さを拡大し、視認性とタップしやすさを改善。
- `npm run build` 成功。公開反映前にGitHub Pagesのビルド状態を確認する。

## 2026-08-16: 中心地点・比較地点の操作を整理

- セクション名を「中心地点」から「地点」へ変更。
- 共通の「地点の種類」を先頭に配置し、中心地点と比較地点で同じ分類を利用。
- 中心地点は「検索 → 選択」の流れに統一。
- 比較は「2地点を比較する」チェックボックスで任意化し、無効時は比較地点の操作を隠す。
- 比較地点にも検索・選択・座標入力・地図クリック指定を用意。
- 中心地点と重複する地点Aの入力欄は削除し、中心地点をAとして扱う。
- 比較結果は比較地点操作の直下に配置。
- `npm run build` 成功。公開反映前にGitHub Pagesのビルド状態を確認する。

## 2026-08-16: 比較有無と地点操作順の表示を修正

- 「地点の種類 → 中心地点を検索 → 中心地点を選択 → 2地点を比較する」の順に修正。
- 比較を無効にしている状態でも「2地点を比較する」チェックボックスを常に表示。
- 比較を有効にした後だけ、比較地点の検索・選択・座標入力・地図指定を表示。
- `npm run build` 成功。

## 2026-08-16: 比較地点の入力案内を改善

- 中心地点・比較地点それぞれに「検索またはプルダウンで選択」と明記。
- 比較地点の経度・緯度入力に個別ラベルを追加。
- 座標入力を経度・緯度の2列レイアウトに変更し、狭い画面での折り返しを改善。
- 「地図でBを指定」を「比較地点を地図で指定」へ変更。
- 地図クリック時の案内文も「比較地点」と表記。
- `npm run build` 成功。

## 2026-08-16: 比較地点の経度・緯度レイアウトを修正

- 座標欄の見出しと地図指定ボタンが1列分として配置されていたCSS指定を修正。
- 経度を左、緯度を右に並べ、見出しとボタンは2列全体に配置。
- `npm run build` 成功。

## 2026-08-16: 中心地点・比較地点の指定方法を統一

- 中心地点にも経度・緯度入力と地図クリック指定を追加。
- 地図上のマーカー表示を「A/B」から「中心／比較」へ変更。
- 中心地点と比較地点の座標入力ブロックを同じスタイルに統一。
- 比較結果の見出しと数値部分を分離し、見出しが途中で折り返されないよう修正。
- `npm run build` 成功。

## 2026-08-16: 比較結果と地図操作セクションの表示改善

- 「2地点比較」を中心・比較・距離・方位の項目別表示へ変更。
- 狭い画面でも見出しや数値が不自然な位置で折り返されないよう調整。
- 「テレメトリ」を「地図操作・情報」へ変更。
- 英語表示は `Map controls & information` とした。
- `npm run build` 成功。

## 2026-08-16: 比較結果と操作値の意味を明確化

- 比較結果の中心地点・比較地点を、地点名がある場合は「名称＋緯度経度」で表示。
- プリセット外の地点は緯度経度のみ表示。
- 「ロール」を「地図の回転」へ変更。
- 「縮尺」を「表示倍率（自動調整=100%）」へ変更。
- 「距離」を「中心からの距離」として表示し、比較結果では「中心から比較地点まで」と明示。
- `npm run build` 成功。

## 2026-08-16: 投影法別の標準表示リセットを追加

- 投影法を変更せずに、現在の投影法の標準的な中心・向きへ戻す「標準表示に戻す」ボタンを追加。
- メルカトル図法などは本初子午線・赤道交点（0°, 0°）を基準にする。
- 正距方位図法は北極点を標準中心とする。
- 正射方位図法は赤道上の0°経線交点（0°, 0°）を基準にする。
- 回転を0°、表示倍率を自動調整へ戻し、比較表示を無効化する。
- `npm run build` 成功。

## 2026-08-16: 地図プリセットを追加

- セクション順を「投影法 → 地図プリセット → 地点 → 地図操作・情報 → レイヤー → テーマ」に整理。
- 「地図プリセット」へ「標準世界図」「日本中心（東経135°）」を追加。
- プリセット適用時も投影法は現在の選択を維持。
- 「標準表示に戻す」ボタンを地図プリセットへ移動。
- `npm run build` 成功。

## 2026-08-16: 操作階層・説明性・表示レイヤーを改善

- セクション順を「投影法 → 情報・操作 → 地図プリセット → 地点 → 表示レイヤー → テーマ」に変更。
- 「プリセットを適用」を廃止し、プリセット選択時に即時適用する方式へ変更。
- 「標準表示に戻す」は現在の投影法の標準状態へ戻す専用ボタンとして分離。
- 北極点・南極点・0°,0°を「基準地点」カテゴリとして追加し、地点選択欄では常時選択可能にした。
- 地図ラベルの種類を都市名スイッチの直前へ移動。
- セクションと主要操作へインフォメーションボタン、ホバー説明、アクセシブルなラベルを追加。
- 中心・比較地点の表示を地図ピン型アイコンへ統一。
- 「高コントラスト」テーマを追加。デフォルトテーマは従来の「クラシック」を維持。
- `npm run build` 成功。

## 2026-08-16: 世界遺産都市フィルタを追加

- 中心地点と地図上の都市名の両方に「世界遺産都市」フィルタを追加。
- 「観光都市」は、既存の観光都市に世界遺産都市を含む条件へ整理。
- UNESCO World Heritage DataHubの公式データを実行時に取得し、登録済み都市から50km以内の世界遺産地点を近接判定して自動分類。
- UNESCOデータを取得できない場合は、主要な世界遺産都市を収録した内蔵フォールバックへ切り替える。
- 自然遺産や都市から遠い遺産は、都市フィルタへ無理に含めない。
- データ出典: https://data.unesco.org/explore/dataset/whc001/table/
- `npm run build` 成功。公開反映はビルド確認後に行う。

## 2026-08-16: 軍事・歴史地点カテゴリを追加

- 中心地点と地図上の都市名の両方に「軍事・歴史地点」フィルタを追加。
- 公開情報として広く知られる要塞・海峡・運河などの要衝、および歴史的な主要戦場を収録。
- 例: ジブラルタル、スエズ、ダーダネルス海峡、ワーテルロー、ゲティスバーグ、関ヶ原、ミッドウェーなど。
- 既存の都市検索・2地点比較でも同じ地点を選択可能。
- 現役軍事施設の詳細な位置情報は扱わず、歴史学習・地理的要衝の比較を目的とする。
- `npm run build` 成功。公開反映前にGitHub Pagesのビルド状態を確認する。

## 2026-08-16: 都市検索・都市データを拡張

- 世界各地域の首都・主要都市を追加。
- 歴史都市・文化都市を検索用データとして追加。
- プルダウンは主要都市中心、検索入力時は全登録都市を対象に変更。
- 日本語名・地域グループを追加。
- 中心地点の分類フィルタと、地図上の都市名分類フィルタを分離。
- 首都・主要都市・歴史都市・港湾都市・観光都市を分類条件として追加。
- 海位機能は再導入していない。

## 2026-08-15: 地形・水深／海面変化機能の開始

### 開始前のGitHubチェックポイント

- リポジトリ: https://github.com/skawanojpn/map-orientation-studio
- 公開先: https://skawanojpn.github.io/map-orientation-studio/
- ベースコミット: `5de8c7c` (`Add equal-area projection guides and map projections`)
- ベース状態: `git status --short` が空であることを確認
- ベース機能: 8種類の投影法、都市表示、2地点比較、投影法解説、GitHub Pages公開

### 今回の目的

既存の投影法を増やすのではなく、標高・水深データと海面高度を現在の地図へ重ねるレイヤー機能を追加する。

### 実装方針

1. 既存地図と投影法を維持する。
2. 地形・水深を低解像度の色分けレイヤーとして描画する。
3. 海面高度を `-125m` から正の値まで変更できるようにする。
4. 設定した海面より低い地点を海域として扱い、変更後の海岸線・水没域を表示する。
5. 高解像度全球データをリポジトリへ直接同梱せず、まずは軽量なサンプル／低解像度データで成立性を確認する。

### 失敗時の復旧

今回の作業で問題が発生した場合は、ベースコミット `5de8c7c` の状態を基準に修正する。既存機能を壊す変更は分離してコミットし、ビルド成功後にのみ公開成果物を更新する。

### リスク・未確定事項

- 全球の高解像度標高・水深データは容量が大きく、GitHub PagesやGASへの直接同梱には不向き。
- まずは表示方式と操作性を検証し、データソース・解像度・タイル配信方式は別途決定する。
- 海面を過去へ戻す単純モデルは教育・可視化用であり、氷床荷重や地殻変動を含む厳密な古地理復元ではない。

## 進捗

- [x] 現状確認
- [x] GitHub上のベースコミット確認
- [x] 作業方針と復旧方針を記録
- [x] 地形・水深レイヤー（撤去済み）
- [x] 海面高度スライダー（撤去済み）
- [x] 現在／氷期／将来プリセット（撤去済み）
- [x] ビルド・型チェック
- [x] GitHub Pages反映確認（公開ビルドのコミット `1296c3f` が `built`）

## 2026-08-15: 海面高度の変化が見えない問題を修正

- 海面高度スライダー／プリセット操作時に地形レイヤーを自動的に有効化。
- 海面上昇で水没する低地、氷期に現れる浅海域、深海を別色で表示。
- 現在の海岸線を白線で重ね、海面変更前後を比較可能にした。
- `npm run build` 成功。

## 2026-08-15: 地形レイヤーが全て青になる問題を修正

- TopoJSON変換後のFeatureラッパーを明示的にGeometryとして`geoContains`へ渡すよう修正。
- 陸地セルと海域セルの判定を分離して保持。
- `npm run build` 成功。コミット: `db058ab`

## 2026-08-16: 海位差分表示を再設計中（未公開）

- 通常の陸地ポリゴンを保持し、海面高度の変更差分だけを別描画する方式へ変更。
- 海岸セルに低い仮想標高を設定し、`+1m`〜`+10m`で水没差分、`-125m`で露出差分が視認できるようにした。
- 海面高度の設定値と、水没セル数・新たな陸地セル数を同じ読出し欄へ表示。
- `npm run build` 成功。
- この変更は未公開。GitHub Pagesへは、ローカルで海位変化を確認してから反映する。

## 2026-08-16: NOAA ETOPO実データ接続（未公開）

- NOAA NCEI ETOPO 2022の60秒全球グリッドから、5度間隔のサンプルを取得する処理を追加。
- 取得成功時はセルの標高・水深をETOPO値へ置換し、取得失敗時は概念モデルへフォールバック。
- UIに「実データ／読み込み中／概念モデル」の状態表示を追加。
- 実データの取得確認と海位差分の表示確認が完了するまで公開しない。

## 2026-08-16: 海位機能をオミット

- 海位・地形水深機能は、実データに基づく信頼できる表示品質へ到達していないため撤去。
- 海面高度UI、地形・水深レイヤー、海位差分メッシュ、ETOPO取得処理を削除。
- 既存の投影法、都市、2地点比較、投影法解説は維持。
- 今後再開する場合は、地域限定の事前処理済み実データを検証してから再設計する。

### 実装メモ

- `地形・水深（概念モデル）`レイヤーを有効にすると、標高・水深を色分けした低解像度メッシュを既存の投影法で描画する。
- 海面高度が変わると、メッシュの海域・陸域判定が変わる。現在の海岸線と比較するための基礎機能であり、実測DEMそのものではない。
- 次の段階では、GEBCO等の標高・水深データを5度メッシュへ変換するインポート処理へ差し替える。
- `npm run build` 成功済み。`dist/`、`docs/`、`gas/Index.html`を生成済み。

## 2026-08-16: 基準地点名とテーマ初期値を改善

- 「ヌル島」を、意味が伝わりやすい「本初子午線・赤道交点（0°, 0°）」へ変更（英語表示も対応）。
- テーマの新規利用時の既定値をNatureへ変更。
- テーマ選択肢の先頭をNatureに変更。
- `npm run build` 成功。

## 2026-08-16: 表示レイヤーの順序を改善

- 表示レイヤーの項目を「都市名」→「国境線」の順に変更。
- `npm run build` 成功。

## 2026-08-16: 地図プリセットを拡張

- 本初子午線中心（標準）、日本中心、太平洋中心を追加。
- オーストラリア視点として、中心をオーストラリア付近に置き、ロールを180度にした南方上表示を追加。
- `npm run build` 成功。
