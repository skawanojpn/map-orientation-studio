# Map Orientation Studio — 開発計画

## 0. 現状

- 新規プロジェクトディレクトリを作成済み。
- `axis-mapping` と `azi-rotate` のソース、README、公開用 `docs` を確認済み。
- 両アプリは依存管理のない単一HTMLで、D3.js／TopoJSONをCDNから読み込む構成。
- 既存アプリは変更せず、統合版を別プロジェクトとして開発する。

## 1. 実装方針の決定

1. プロジェクト名・ライセンス・対応ブラウザを確定する。
2. Vercelを主公開先、GitHub Pagesを代替公開先として設定する。
3. GAS版は同じフロントエンドをHtmlServiceで配信し、GAS依存コードを分離する。
4. 初期実装はTypeScript対応の小規模フロントエンド構成に移行する。過度なフレームワーク依存は避け、Canvas描画とD3地理機能を中心にする。

## 2. MVPの骨格を作る

1. `src/`を次の責務に分ける。
   - `app`: 起動、状態、イベント
   - `map`: 投影アダプター、描画、レイヤー
   - `data`: 都市、地図データ、GeoJSON／CSV変換
   - `ui`: サイドバー、設定、翻訳
   - `export`: PNG／将来のSVG出力
2. アプリ状態を1つの型で管理する。
3. `MercatorAdapter` と `AzimuthalEquidistantAdapter` を実装する。
4. 共通の描画順を確立する。
   - 背景 → 海面 → 標準グリッド → 国土 → 国境 → 投影固有グリッド → 中央軸／等距離円 → 地点 → ラベル
5. 既存の5テーマ、日英翻訳、都市プリセットをデータファイル化する。

## 3. 操作性と可視化を洗練する

1. 中心経度・緯度・ロール・縮尺の入力を双方向同期する。
2. ドラッグ、Shift+ドラッグ、ホイール、ピンチを投影法共通の操作APIへ接続する。
3. 投影法切替時に中心地点とロールを維持し、必要なら縮尺を自動調整する。
4. 現在の中心地点、カーソル地点、距離、方位を情報パネルに整理する。
5. モバイル用の折りたたみ可能な操作パネルを用意する。
6. 初回表示時に短い操作ガイドを表示し、閉じた後は設定から再表示できるようにする。

## 4. 追加地図表示方法の検討と実装順

### 優先度A：MVP後すぐに検証

- 正距円錐図法、正積円筒図法、正射方位図法の追加。
- 大圏航路と等角航路の描画。
- 2地点間の距離・初期方位・終端方位の比較。
- Tissotの指示楕円による局所的な歪みの可視化。

### 優先度B：学習・分析用途

- 歪みの種類ごとの色分け（距離、面積、角度、方向）。
- 日照境界線、タイムゾーン境界、経度0度・日付変更線。
- 地形・海岸線など、追加データレイヤーの切替。
- 地点集合のインポートとラベル重なり回避。

### 優先度C：共有・制作用途

- URLクエリまたはハッシュへの状態保存。
- プリセットのJSON書き出し／読み込み。
- SVG、透過PNG、印刷用高解像度画像の出力。
- 2投影の左右比較、または同一投影の時系列スライダー。

## 5. データとGAS連携

1. 初期地図データは既存同様World Atlasを使用し、読み込み失敗UIを実装する。
2. データURLを設定値に切り出し、将来のローカル同梱や別ホストへ切替可能にする。
3. GAS版の最小構成を作る。
   - `Code.gs` の `doGet`
   - HTMLテンプレート配信
   - 必要なら `google.script.run` でユーザー設定を保存・取得
4. 静的版ではlocalStorageまたはURL状態、GAS版ではUserProperties／スプレッドシートなど、保存先をAdapterで切り替える。
5. APIキーや秘密情報をフロントエンドへ埋め込まない。

## 6. 品質確認

### 機能

- 世界地図、国境、都市、グリッド、中央軸、等距離円が各トグルで正しく切り替わる。
- 都市選択、数値入力、ドラッグ、Shift操作、ホイール、ピンチが相互に矛盾しない。
- 投影法を切り替えても中心・ロール・言語・テーマが保持される。
- PNG出力が現在のテーマ・表示レイヤー・高DPI設定を反映する。

### 境界条件

- 緯度±90度、経度±180度、ロール360度超、日付変更線、極点、極端な縮尺。
- 小画面、横向き、Retina／高DPI、キーボードのみの操作。
- 地図データ取得失敗、遅延、再読み込み。
- GASの権限確認、Webアプリ実行ユーザー設定、外部CDN制約。

## 7. 公開

1. `npm run build`または同等の静的生成を確認する。
2. VercelでPreviewを作り、主要ブラウザで確認する。
3. GitHub Pages向けの静的出力を用意する。
4. GASのテストデプロイを作り、静的版と同じ受入項目を確認する。
5. READMEに起動、ビルド、Vercel、GitHub Pages、GASの公開手順を記載する。

## 8. 次の着手順

1. TypeScript／ビルド方式の最小構成を作る。
2. 既存2アプリからデータ定義とテーマ定義を移行する。
3. 投影アダプターと共通状態を実装する。
4. Canvas描画と操作を移行する。
5. レイヤー比較UI、追加投影法、GAS版の順に進める。

## 9. 実装済み（2026-08-15）

- Vite＋TypeScriptによる実行可能なフロントエンド骨格を追加。
- メルカトル、正距方位、Equal Earth、正射方位、正距円筒の5投影を追加。
- 共通の中心・ロール・縮尺状態、レイヤー、テーマ、日英切替を実装。
- ドラッグ、Shift＋ドラッグ、ホイール、2本指ピンチに対応。
- URLハッシュへ表示状態を保存し、共有URLから復元できるようにした。
- Viteの生成物をGAS HtmlService用にインライン化する`build-gas.mjs`を追加。
- `gas/Code.gs`、`gas/Index.html`、`gas/appsscript.json`を追加。
- `npm run build`でTypeScript検査、静的ビルド、GAS HTML生成まで成功。

## 10. 次期ファインチューニング計画

### 10.1 正射方位図法の都市ラベル可視性

#### 方針

正射方位図法では、Canvas内に投影点が返ってきても「地球の裏側にある都市」を表示しない。投影点の画面内判定だけに依存せず、中心地点から都市までの球面距離が90度以内かを判定する。

```text
center → city の球面距離 <= 90°
かつ projection(city) が存在
かつ画面内
```

正射方位図法以外では、各投影のクリップ結果と画面内判定を使う。将来的にはProjection Adapterに次の責務を持たせる。

- `isVisible(coord, state)`
- `project(coord)`
- `defaultScale(viewport)`
- `descriptionKey`

#### 追加する表示制御

- 「表示範囲内の都市のみ表示」をデフォルトで有効化。
- 「全都市ラベル」切替は、必要な場合だけ表示する上級設定にする。
- 縮尺に応じてラベル数を減らす。小縮尺では主要都市のみ、中・大縮尺で全都市を表示。
- ラベルが重なる場合は、重要度順に残して重なりを抑制する。

#### 受入条件

- 正射方位図法で裏側の都市名と点が表示されない。
- 中心地点を変更すると表示都市が正しく入れ替わる。
- 境界付近（90度付近）でラベルがちらつきにくい。
- 他の投影法の都市表示には意図しない制限がかからない。

### 10.2 都市データの拡充

#### データ設計

都市を単純な配列から、次の属性を持つデータへ拡張する。

- `id`
- 英語名／日本語名
- 経度・緯度
- 地域、国、タイムゾーン
- 表示優先度（capital / major / standard）
- 任意の説明、人口規模、標高

#### UI

- 地域別グループ（日本、東アジア、東南アジア、欧州、南北アメリカ、アフリカ、中東、オセアニア）。
- 都市名検索。
- 「主要都市のみ」「全都市」フィルター。
- 地図上の都市をクリックしたときの詳細ポップオーバー。
- URL状態には選択都市IDと都市表示モードを保存する。

#### 段階導入

1. 既存都市を完全な日英データへ移行する。
2. 日本・東アジアの都市を優先して追加する。
3. 全世界の主要都市を追加する。
4. 外部CSV／GeoJSONインポートを上級機能として追加する。

都市を増やし過ぎるとラベルが読みにくくなるため、データ量の増加と表示密度を分離する。内部には多く保持し、表示側で優先度・縮尺・重なりを制御する。

### 10.3 投影法解説への導線

#### 推奨UI

地図を覆う大きなモーダルを常用せず、投影法セレクトの横に「i」ボタンを置く。クリックすると右側またはサイドバー内に解説ドロワーを開く。

```text
投影法セレクト [正射方位図法] [i]
                         ↓
              投影法解説ドロワー
              ・何を正しく保つか
              ・どこが歪むか
              ・向いている用途
              ・現在の表示で見るポイント
```

#### 解説の構成

- 1行要約：例「地球儀を遠くから見たように表示する投影法」
- 保持する性質：距離、角度、面積、方位、外観
- 注意点：歪み、表示範囲、極点・境界の挙動
- 用途例：教材、航路、地球の外観、比較分析
- 「この投影で試す」ボタン：解説から投影法を切り替える
- 「他の投影と比較」ボタン：比較モードへの導線

#### 初回利用者向け導線

- 初回のみ短い3ステップガイドを表示する。
  1. 中心地点を選ぶ
  2. 投影法を変える
  3. グリッドや等距離円で性質を観察する
- 2回目以降は表示せず、設定から再表示できるようにする。
- 解説は日本語／英語で同じ構造を保つ。

### 10.4 可視化・比較機能

優先度Aとして、次を追加する。

- 2地点間の距離、初期方位、終端方位。
- 大圏航路と等角航路の重ね合わせ。
- Tissotの指示楕円による局所歪み表示。
- 2つの投影法を左右に並べる比較ビュー。
- 表示中の投影法の性質を簡潔に示す凡例。

優先度Bとして、日照境界線、タイムゾーン、GeoJSON／CSV地点インポート、SVG出力を追加候補とする。

## 11. 次回の実装単位

次回はリスクが低く効果が大きい順に、次の1スプリントで進める。

1. Projection Adapterに`isVisible`を追加し、正射方位図法の裏側都市を確実に非表示にする。
2. 都市データを日英名・地域・重要度付きへ移行する。
3. 都市検索と「主要都市のみ／全都市」表示モードを追加する。
4. 投影法選択横の解説ドロワーを追加する。
5. 既存機能の操作・URL共有・GAS生成・静的ビルドを回帰確認する。

## 12. 実装済み（次期スプリント）

- 正射方位図法で中心から90度以上離れた都市を非表示にした。
- 都市プリセットに地域グループを追加した。
- 主要都市のみ／全都市の表示モードを追加した。
- 都市名検索を追加した。日本語表示中は日本語名でも検索できる。
- 投影法名と都市名の日本語切替を実装した。
- 投影法セレクト横に解説ボタンを追加し、投影法の性質・歪み・用途を表示するドロワーを追加した。
- ビルド、`dist/`、`docs/`、GAS用HTMLの生成を確認した。

## 13. 実装済み（分析・学習機能）

- 中心地点の意味、ロール／軸の意味、極を中心にする意味を投影法解説ドロワーへ追加した。
- 日本・東アジア・南アジア・欧州・南北アメリカ・アフリカ・中東・オセアニアの主要都市を追加した。
- 2地点の距離と初期方位を表示する比較地点を追加した。
- 大圏航路レイヤーを追加した。
- 投影による歪みを観察する指示円レイヤーを追加した。
- GitHub Pages／GAS成果物を含むビルドとローカルGit初回コミットを確認した。

## 14. デプロイ状況

- ローカルGitリポジトリとコミットは作成済み。
- `docs/`、`dist/`、`gas/`の公開成果物は生成済み。
- GitHub CLIは既存アカウントの認証トークンが無効で、Vercel CLIは未導入のため、外部サービスへの実デプロイは認証待ち。
